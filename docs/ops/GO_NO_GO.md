# Go/No-Go — Критерии принятия Фазы 0

**Дата:** 2025-11-12  
**Статус:** Финальный чеклист для завершения Фазы 0

---

## Обзор

Данный документ содержит критерии для принятия решения о завершении Фазы 0 и переходе к Фазе 1.

---

## ✅ Обязательные критерии

### 1. Контракты + Smoke/E2E зелёные на preview/staging

**Проверка:**
- [ ] PR checks проходят успешно (lint, typecheck, build)
- [ ] OpenAPI спецификации валидируются через Spectral
- [ ] Contract tests (headers, idempotency) проходят на preview
- [ ] Smoke tests проходят на staging
- [ ] E2E tests проходят на preview
- [ ] Все артефакты (логи, скриншоты, видео) сохраняются в CI

**Команды для проверки:**
```bash
# Локально
pnpm lint
pnpm typecheck
pnpm build
pnpm validate:openapi
pnpm test:contracts
pnpm test:smoke:staging
pnpm test:e2e

# В CI
# Проверить последний PR → Actions → PR Checks
# Проверить последний деплой → Actions → Preview Deploy / Staging Deploy
```

**Критерий принятия:** Все тесты зелёные, нет критичных ошибок.

---

### 2. Edge-кэш включён, приватка — всегда no-store

**Проверка:**
- [ ] Публичные GET endpoints возвращают `Cache-Control: public, s-maxage=300, stale-while-revalidate=60`
- [ ] Приватные endpoints возвращают `Cache-Control: no-store, no-cache, must-revalidate`
- [ ] Contract tests для заголовков кэша проходят
- [ ] Cloudflare Cache Rules настроены (если применимо)

**Команды для проверки:**
```bash
# Публичные endpoints
curl -I https://api-staging.go2asia.space/v1/api/content/countries
# Должен быть: Cache-Control: public, s-maxage=300, stale-while-revalidate=60

# Приватные endpoints
curl -I https://api-staging.go2asia.space/v1/api/auth/me
# Должен быть: Cache-Control: no-store, no-cache, must-revalidate

# Contract tests
pnpm test:contracts
```

**Критерий принятия:** Все заголовки кэша корректны, тесты проходят.

---

### 3. Lighthouse ≥ 85 на ключевых страницах

**Проверка:**
- [ ] Lighthouse CI настроен в `.github/lhci.json`
- [ ] Lighthouse запускается на preview и staging
- [ ] Performance ≥ 85 на всех ключевых страницах
- [ ] SEO ≥ 85 на всех ключевых страницах
- [ ] Best Practices ≥ 85 на всех ключевых страницах
- [ ] Accessibility ≥ 80 (warning, не error)

**Страницы для проверки:**
- `/` (главная)
- `/atlas` (список стран)
- `/blog` (список статей)
- `/pulse` (список событий)
- `/atlas/countries/{id}` (детальная страница страны)
- `/blog/{slug}` (детальная страница статьи)

**Команды для проверки:**
```bash
# Локально (если установлен Lighthouse CI)
lhci autorun --config=.github/lhci.json

# В CI
# Проверить последний PR → Actions → Preview Deploy → Lighthouse CI
# Проверить последний деплой → Actions → Staging Deploy → Lighthouse CI
```

**Критерий принятия:** Все метрики ≥ 85, артефакты Lighthouse сохранены в CI.

---

### 4. Алёрты заведены, SLO/SLI дашборды есть, runbooks готовы

**Проверка:**
- [ ] Cloudflare Alerts настроены (см. `docs/ops/ALERTS.md`)
  - [ ] 5xx spike > 1% за 5 мин
  - [ ] Latency p95 > 300ms за 5-10 мин
  - [ ] Timeouts/retries > N за 5-10 мин
  - [ ] Rate limit hits (429) на публичных ручках
- [ ] SLO/SLI дашборды созданы в Cloudflare (см. `docs/ops/SLO_SLI.md`)
  - [ ] Availability (30 дней)
  - [ ] Error Rate
  - [ ] Latency p95
  - [ ] Throughput
  - [ ] 429 Rate
  - [ ] Timeouts
- [ ] Runbooks готовы (см. `docs/ops/RUNBOOKS.md`)
  - [ ] Всплеск 5xx ошибок (P0)
  - [ ] Рост p95 латентности (P0)
  - [ ] Таймауты к сервисам (P0)
  - [ ] Падение Clerk JWKS (P0)
  - [ ] Проблемы с БД
  - [ ] Превышение rate limit

**Ручная настройка:**
1. Cloudflare Dashboard → Analytics → Alerts → Создать алёрты согласно `docs/ops/ALERTS.md`
2. Cloudflare Dashboard → Analytics → Custom Dashboard → Создать виджеты согласно `docs/ops/SLO_SLI.md`
3. Проверить что все runbooks документированы в `docs/ops/RUNBOOKS.md`

**Критерий принятия:** Все алёрты настроены, дашборды созданы, runbooks готовы.

---

### 5. PITR/rollback проверены

**Проверка:**
- [ ] Neon PITR включён для staging и production
- [ ] Branch restore проверен в Neon (создание ветки, тест, удаление)
- [ ] Rollback миграций работает (`db:migrate:down`)
- [ ] Rollback деплоя работает (через Cloudflare Dashboard → Workers → Versions)
- [ ] Логи миграций сохраняются как артефакты в CI
- [ ] Документация по backup/recovery готова (`docs/ops/BACKUP_RECOVERY.md`)

**Команды для проверки:**
```bash
# Проверить статус миграций
cd services/content-service
DATABASE_URL=$STAGING_DATABASE_URL pnpm db:migrate:status

# Проверить rollback миграций
DATABASE_URL=$STAGING_DATABASE_URL pnpm db:migrate:down

# Проверить branch restore в Neon
# Neon Dashboard → Project → Branches → Create Branch → Test → Delete
```

**В CI:**
- Проверить что логи миграций сохраняются в artifacts
- Проверить что production workflow имеет rollback логику

**Критерий принятия:** Все процедуры backup/recovery проверены и документированы.

---

## 📋 Дополнительные проверки

### Инфраструктура

- [ ] Cloudflare аккаунты созданы (staging, production)
- [ ] Neon проекты созданы (staging, production)
- [ ] Netlify сайты созданы (preview, staging, production)
- [ ] GitHub Secrets настроены (см. `docs/ops/CI_CD_SETUP.md`)
- [ ] Environment protection настроен для staging/production

### Документация

- [ ] `docs/ops/CI_CD_SETUP.md` — актуальна
- [ ] `docs/ops/DATABASE_SETUP.md` — актуальна
- [ ] `docs/ops/BACKUP_RECOVERY.md` — актуальна
- [ ] `docs/ops/ALERTS.md` — актуальна
- [ ] `docs/ops/SLO_SLI.md` — создана
- [ ] `docs/ops/RUNBOOKS.md` — актуальна
- [ ] `docs/ops/EXIT_CRITERIA.md` — актуальна
- [ ] `README.md` — обновлён с прогрессом Фазы 0

### Код

- [ ] Все сервисы имеют `/health` и `/ready` endpoints
- [ ] Все сервисы логируют с PII-маскированием
- [ ] Все сервисы используют только `jose` для JWT
- [ ] Gateway имеет rate limiting и кэширование
- [ ] Все миграции атомарны и отслеживаются
- [ ] Все seed файлы в UTF-8

---

## 🚦 Решение Go/No-Go

### Go ✅

**Условия:**
- Все обязательные критерии выполнены
- Нет критичных багов
- Документация актуальна
- Инфраструктура настроена

**Действия:**
1. Обновить `README.md` с отметкой о завершении Фазы 0
2. Создать GitHub Release с тегом `v0.1.0`
3. Начать Фазу 1 согласно плану

### No-Go ❌

**Условия:**
- Есть невыполненные обязательные критерии
- Есть критичные баги
- Документация не актуальна
- Инфраструктура не настроена

**Действия:**
1. Составить список невыполненных критериев
2. Приоритизировать задачи
3. Выполнить недостающие задачи
4. Повторить проверку Go/No-Go

---

## 📊 Метрики готовности

### Процент выполнения

```
Обязательные критерии: X/5 выполнено
Дополнительные проверки: X/Y выполнено
Общий прогресс: X% готовности
```

### Типичные проблемы и решения

1. **Lighthouse не проходит**
   - Проверить оптимизацию изображений
   - Проверить минификацию CSS/JS
   - Проверить использование CDN

2. **Алёрты не настроены**
   - Следовать инструкциям в `docs/ops/ALERTS.md`
   - Использовать Cloudflare Dashboard для настройки

3. **PITR не проверен**
   - Создать тестовую ветку в Neon
   - Выполнить тестовый запрос
   - Удалить тестовую ветку

4. **Contract tests падают**
   - Проверить заголовки в Gateway
   - Проверить формат ответов
   - Обновить тесты если необходимо

---

## ✅ Финальная проверка

Перед принятием решения о завершении Фазы 0:

1. [ ] Пройти все обязательные критерии
2. [ ] Выполнить все дополнительные проверки
3. [ ] Проверить документацию
4. [ ] Проверить инфраструктуру
5. [ ] Получить подтверждение от команды

---

**Последнее обновление:** 2025-11-12

